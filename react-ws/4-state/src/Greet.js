const Greet = ({msg}) => {

   // let msg=props.msg;

    return (
        <div>

        <h1>Greet Component</h1>
        <h3>message: {msg}</h3>
            
        </div>
    );
};

export default Greet;